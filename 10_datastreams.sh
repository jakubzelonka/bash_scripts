#!/bin/bash

# > or 1> forward stdout into file
# 2> forward stderr into file
# &> forward stdout and stderr to file

# when forward something to /dev/null -> whipe the stuff completely
# >> append to file, > overwrite file
